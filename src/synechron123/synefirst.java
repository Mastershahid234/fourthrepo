package synechron123;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;

/*
 * public class synefirst {
 * 
 * @Test public void first() {
 * System.setProperty("webdriver.chrome.driver","./driver/chromedriver.exe");
 * WebDriver driver=new ChromeDriver();
 * driver.manage().timeouts().implicitlyWait(500,TimeUnit.SECONDS);
 * driver.get("https://www.flipkart.com/"); driver.manage().window().maximize();
 * driver.findElement(By.xpath("//button[text()='✕']")).click(); //Actions s=new
 * Actions(driver); WebElement
 * ele=driver.findElement(By.xpath("//div[text()='Grocery']")); ele.click();
 * //s.moveToElement(ele).perform(); //WebElement
 * ele1=driver.findElement(By.xpath("//div[text()='Delivery to 560093']"));
 * //s.moveToElement(ele1);
 * //driver.findElement(By.xpath("//input[@name='pincode']")).clear();
 * driver.findElement(By.xpath("//input[@name='pincode']")).sendKeys("560090");
 * driver.findElement(By.xpath("//span[text()='Login']")).click();
 * //driver.close(); //a[text()='All']
 * 
 * }
 * 
 * }
 */
